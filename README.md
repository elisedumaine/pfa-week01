Animation Project File Report scans a folder and lists its files, file types, sizes, and possible gaps in numbered image sequences.
Run it from the folder containing `yourtool.py` with `python3 yourtool.py`, or pass a folder path such as `python3 yourtool.py /path/to/project`.
In a future version, I would add recursive folder scanning and export the report as CSV.
